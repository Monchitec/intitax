export const bookingFormStyles = {
  container:
    "mx-auto max-w-4xl tv:max-w-6xl 3xl:max-w-7xl px-4 sm:px-6 lg:px-8 tv:px-12 pt-0 pb-6 sm:pb-10",
  backLink:
    "text-muted-foreground hover:text-foreground mb-4 inline-flex items-center gap-2 text-xs sm:text-sm font-medium transition-colors cursor-pointer",

  // Header & Title
  headerWrapper: "mb-6 text-center",
  badge:
    "border-primary/20 bg-primary/5 text-primary mb-2.5 inline-flex items-center gap-2 rounded-full border px-3 py-0.5 text-xs font-bold uppercase tracking-wider",
  title: "text-foreground text-2xl font-black tracking-tight sm:text-4xl",
  subtitle:
    "text-muted-foreground mt-2 text-xs sm:text-sm leading-relaxed max-w-2xl mx-auto",

  // Calendly Container
  calendlyCard:
    "rounded-3xl border border-border/80 bg-card p-2 sm:p-4 shadow-xl overflow-hidden min-h-[700px]",
  calendlyIframe: "w-full min-h-[680px] sm:min-h-[720px] rounded-2xl border-0",

  // Trust Badges Footer
  trustFooter:
    "mt-8 flex flex-wrap items-center justify-center gap-6 text-xs text-muted-foreground",
  trustItem: "inline-flex items-center gap-2 font-medium",
};
